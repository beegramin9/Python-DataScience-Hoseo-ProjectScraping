https://book.coalastudy.com/data-crawling/week-6/stage-1

정적 수집(Static Crawling): W5까지의 수집방식 
requests 라이브러리로 주소를 통하여 단발적으로 접근
접속을 유지하는 것이 아닌 단 한 번 요청하여 HTML을 받아오는 방식
따라서 로그인이 필요하거나 데이터가 실시간으로 로딩되는 경우는 수집이 불가능하다
사람이 아닌 코드가 데이터를 요구하는 것을 감지하여 차단하는 경우도 있음
그러나 빠르다

동적 수집(Dynamic Crawling)
Selenium은 실제 브라우저를 통하여 인간이 입력하고 클릭하는 것과 똑같이 작동한다.
코드만으로 해결하는 것 보다는 느리지만, 앞의 한계를 모두 다 뛰어넘을 수 있는 강력한 방법

정적 수집을 사용할 수 있는 경우:
Naver Tv - 한 페이지에 원하는 데이터가 모두 담겨져 있을 때
Naver News, Hashcode - 여러 페이지를 이동하며 수집했지만 query(요청값)만 바꿔 준다면
첫 번쨰 형태를 여러 번 반복하는 것일 뿐인 형태

결론: 
별다른 절차(로그인) 없이 주소로 바로 접속해도 데이터를 볼 수 있을 때 
새로고침 하지 않으면 페이지 안의 데이터가 변하지 않을 때 -> 데이터가 정적일 때

정적 수집을 사용할 수 없는 경우:
네이버 메일함, 틴더 - 반드시 로그인을 거쳐야만 정보를 볼 수 있을 때
메인 페이지를 통하지 않고 접속하면(로그인을 하지 않고 접속하면) 이용할 수 없는 페이지

인스타그램 - 새로고침을 하지 않고도 데이터를 추가로 불러올 수 있음
계정 페이지에서 스크롤이 끝까지 도달하려 하면 사진을 자동적으로 더 불러온다.
게시물을 클릭했을 때 화살표를 누르면 새로운 사진이 계속 로딩된다.
이런 동적 로딩 데이터들은 최초 한 번 밖에 얻지 못하는 정적 수집으로는 도달이 불가능하다.

